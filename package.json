{
  "name": "train",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "prepare:data": "python3 scripts/build_catalog_data.py",
    "dev": "npm run prepare:data && astro dev",
    "build": "npm run prepare:data && astro build",
    "preview": "astro preview",
    "check": "astro check",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^6.2.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@types/node": "^24.9.1",
    "typescript": "^5.9.3"
  }
}
